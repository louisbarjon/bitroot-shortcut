# setup.py
from setuptools import setup, find_packages

setup(
    name='bitroot-shortcut',
    version='0.1.0',
    packages=find_packages(),
    author='louisbarjon', # Replace with your name/username
    author_email='your_email@example.com' # Replace with your email
)